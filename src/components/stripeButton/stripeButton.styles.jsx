import styled from "styled-components";
import StripeCheckout from "react-stripe-checkout";

export const StripeCheckoutButtonContainer = styled(StripeCheckout)`
  margin-left: auto;
  margin-top: 50px;
`;
